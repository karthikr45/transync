// UI standard: UI-STANDARDS.json (enforced by npm run ui:check).
import FeatureUnavailable from "@/components/FeatureUnavailable";

export default function Page() {
  return <FeatureUnavailable title="alerts" />;
}
